# MinIO Dockerfile
# This Dockerfile extends the official MinIO image
# You can customize this file to add additional tools or configurations if needed

FROM minio/minio:latest

# MinIO official image already includes curl and all necessary tools
# Add any custom configurations here if needed in the future

# Expose MinIO S3 API and Console ports
EXPOSE 9000 9001

# MinIO uses its default entrypoint and command
# Command can be overridden in docker-compose.yml

